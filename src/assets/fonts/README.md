# 字体资源

当前版本没有打包 Web Font，字体回退链统一定义在 `src/config/VisualTheme.ts`：

- 正文优先使用思源宋体、系统宋体等衬线中文字体
- 标题使用 Georgia 与中文衬线字体回退
- 设置和提示使用系统无衬线中文字体
- 按键提示使用等宽字体

后续加入本地字体时必须确认网页嵌入、再分发及商业发行授权，并保留系统字体回退，避免字体加载失败导致中文缺字。
